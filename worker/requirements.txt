# FastAPI & Web Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0

# JAX (CPU版 - GPU版が必要な場合は下記のコメントを外してください)
jax[cpu]==0.4.35
# GPU版が必要な場合は以下をコメント解除（CUDA 12.x用）
# jax[cuda12]==0.4.35

# NumPyro (Bayesian inference)
numpyro==0.15.2

# Additional scientific libraries
numpy==1.26.4
scipy==1.14.1

# Logging
python-json-logger==2.0.7

# Optional: ArviZ for Bayesian visualization
# arviz==0.19.0

# Optional: scikit-learn
# scikit-learn==1.5.2

# Optional: PyTorch (if needed)
# torch==2.4.0

# Development dependencies (optional)
# pytest==8.3.3
# pytest-asyncio==0.24.0
# httpx==0.27.2
